Rate this paper for a literature review. Return only valid JSON, no other text.

{"contribution": "<one sentence>", "relevance_justification": "<two sentences>", "relevance_score": <1-10>, "specificity_score": <1-10>}

relevance_score: how directly the paper's own contribution addresses the query topic, using this fixed scale (apply it the same way regardless of the query):
  10: the paper's central contribution is this exact topic; it would be a primary/foundational citation for the query
  8-9: the paper substantially addresses the query topic, e.g. the same problem in a different application domain or via a different specific method
  6-7: the paper is related (shares techniques, domain, or terminology with the query) but its main contribution targets a different problem
  4-5: the query topic appears only as motivation, background, or a minor component; the paper's core contribution is on a different topic
  2-3: the paper is from a broadly related field but does not meaningfully engage with the query's specific concern
  1: the paper is unrelated to the query

specificity_score: how narrowly scoped the paper's contribution is, using this fixed scale:
  10: a single technique, dataset, model, or problem instance
  8-9: a specific method or application with a clearly bounded scope
  6-7: a moderately broad topic spanning several related techniques or applications
  4-5: a survey, benchmark, or review covering a subfield
  1-3: a broad survey or position paper spanning an entire research area
---
Query: {query}
Title: {title}
Abstract: {abstract}
